<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clubscse">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".to_be_attended"></activity>
        <activity android:name=".attended" />
        <activity android:name=".Registration" />
        <activity android:name=".ShowEvent" />
        <activity
            android:name=".navigation_timeline"
            android:label="@string/title_activity_navigation_timeline"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Cart" />
        <activity android:name=".Signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>